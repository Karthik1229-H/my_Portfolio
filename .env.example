VITE_APP_TITLE=Karthik's Portfolio

# SMTP config for sending contact form emails (fill before running server)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password

# Recipient email (where contact messages will be sent). Default: SMTP_USER
TO_EMAIL=omsiva.karthik.2002@gmail.com
